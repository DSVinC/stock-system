# TASK_FIX_001 - 选股/监控池功能修复

**优先级**: P0 (阻塞后续测试)
**创建时间**: 2026-03-23 18:40
**报告人**: 主人
**状态**: 🟡 部分修复

---

## 问题描述

### 问题 1: 选股方向得分异常 ⏳ 分析中

**现象**: 所有方向得分都是 80，细分项都是"社会 5、政策 5、舆论 1、商业 5"

**分析结果**:
- API 返回数据正常，不同方向有不同原始数据
- 评分逻辑正确，但阈值导致结果相似
- 可能原因：数据源差异不够大，或前端显示缓存

**下一步**: 检查数据源和前端缓存

---

### 问题 2: 监控池添加失败 ✅ 已修复

**现象**: 点击"添加监控池"成功跳转，但监控池始终为空

**根本原因**: `/api/monitor-pool/add` API 未实现

**修复内容**:
1. 创建 `api/monitor-pool.js` 模块
2. 实现 5 个 API 端点：
   - POST /api/monitor-pool/add - 添加股票
   - GET /api/monitor-pool/list - 获取列表
   - DELETE /api/monitor-pool/remove - 移除
   - POST /api/monitor-pool/batch-add - 批量添加
   - DELETE /api/monitor-pool/batch-remove - 批量移除
3. 在 server.js 中注册路由

**测试结果**:
- ✅ 监控池列表 API 返回 3 条记录
- ✅ 添加股票 API 成功写入数据库

---

## 验收标准

- [ ] 不同股票显示不同评分 ⏳
- [x] 添加监控池后页面显示已添加的股票 ✅
